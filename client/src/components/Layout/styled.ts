import { flexCenter } from "@/styles/flex";
import styled from "@emotion/styled";

const Container = styled.div`
  ${flexCenter}
  max-width: 100%;
  margin-left: 5%;
  margin-right: 5%;
  margin-top: 8rem;
`;

export const S = {
  Container,
};
